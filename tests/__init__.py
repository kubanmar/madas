#from os.path import dirname, basename, isfile
#import glob
#fnames = glob.glob(dirname(__file__)+"/*.py")
#tests = [ basename(f)[:-3] for f in fnames if isfile(f) and not f.endswith('__init__.py') and not f.endswith('main.py')]

#import simdatframe

#import os

#test_file_path = os.path.split(__file__)
#test_data_path = os.path.join(test_file_path[:-1])

# find absolute path of package on disc and iterate paths relative to this

